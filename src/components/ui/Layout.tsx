import React from "react";
import { Typography } from "@material-ui/core";

const Layout = () => {
  return (
    <div>
      <Typography component="h1" variant="h4">
        Welcome to React TypeScript with Material UI
      </Typography>
    </div>
  );
};

export default Layout;
